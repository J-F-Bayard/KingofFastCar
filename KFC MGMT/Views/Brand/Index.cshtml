@model IEnumerable<KFC_MGMT.Controllers.GetBrandForm>

@{
    ViewBag.Title = "Index";
}


<p>
    @Html.ActionLink("Nouvelle marque", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.IdBrand)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.IdBrand)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.IdBrand }) |
            @Html.ActionLink("Détails", "Details", new { id=item.IdBrand }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.IdBrand })
        </td>
    </tr>
}

</table>
